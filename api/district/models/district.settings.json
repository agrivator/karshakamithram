{
  "kind": "collectionType",
  "collectionName": "districts",
  "info": {
    "name": "district"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "panchayats": {
      "via": "district",
      "collection": "panchayats"
    }
  }
}
